// initial state
const state = () => ({
  user: {
    activeEvent: ["1", "2", "3"],
    historyEvent: ["1", "2", "3"],
  },
});

// getters
const getters = {};

// actions
const actions = {};

// mutations
const mutations = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
